PORT=8000
DB_USER=""
DB_PASSWORD=""
DB_HOST=""
DB_NAME=""
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}/${DB_NAME}?schema=public&connection_limit=3&application_name=web"

FRONTEND_URL=https://marvelcord.com

NODE_ENV=development

DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DISCORD_REDIRECT_URI=https://polls.marvelcord.com/api/v1/auth/callback
DISCORD_BOT_TOKEN=

GUILD_ID=

EXPRESS_SESSION_SECRET=
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Redis Configuration (optional - enables persistent sessions)
# Set REDIS_ENABLED=true to use Redis for session storage
# If not enabled, file-based session storage will be used as fallback
REDIS_ENABLED=false
REDIS_URL=redis://localhost:6379

# Bot service token (shared with the Discord bot; openssl rand -hex 32).
# Optional: while unset, /api/v1/bot/* returns 503 "not configured".
BOT_SERVICE_TOKEN=
